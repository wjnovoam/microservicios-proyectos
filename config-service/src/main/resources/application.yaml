server:
  port: 8091
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/wjnovoam/microservicios-proyectos
          search-paths: config-data
  application:
    name: config-service